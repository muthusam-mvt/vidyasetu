services:
  # =======================================================
  # 1. FastAPI + ChromaDB Backend Service
  # =======================================================
  - type: web
    name: vidya-setu-backend
    runtime: python
    rootDir: backend
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: WEB_CONCURRENCY
        value: "1"
      - key: MALLOC_ARENA_MAX
        value: "2" # Controls memory fragmentation on 512MB Linux containers
      - key: LLM_PROVIDER
        value: groq
      - key: GROQ_MODEL
        value: openai/gpt-oss-120b
      - key: GROQ_API_KEY
        sync: false # Render dashboard will prompt for this secret
      - key: OPENAI_API_KEY
        sync: false
      - key: EMBEDDING_MODEL
        value: sentence-transformers/all-MiniLM-L6-v2
      - key: CHROMA_PERSIST_DIR
        value: ./chroma_store
      - key: FRONTEND_ORIGIN
        value: "*"

  # =======================================================
  # 2. Next.js Vintage Frontend Service
  # =======================================================
  - type: web
    name: vidya-setu-frontend
    runtime: node
    rootDir: frontend
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.14.0
      - key: BACKEND_HOST
        fromService:
          type: web
          name: vidya-setu-backend
          property: host
      - key: BACKEND_PORT
        fromService:
          type: web
          name: vidya-setu-backend
          property: port
      - key: NEXT_PUBLIC_API_BASE
        sync: false
